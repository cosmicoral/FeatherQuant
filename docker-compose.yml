services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: featherquant
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - featherquant_postgres:/var/lib/postgresql/data

volumes:
  featherquant_postgres: